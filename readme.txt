- common.css в нем все общие стили которые используются на каждой странице, он всегда подключен.
- Так же, для каждой страницы есть файл стилей, которые можно перенести в админку для конкретной страницы, или перенести в один файл с html разметкой, если нужно.(например стили для форм с логикой)
- Сейчас подключил календарь (datepicker.min.js), его можно заменить на любой другой если нужно.
Так же нужно будет попробовать настроить его что бы нельзя было выбирать дату ранее сегодняшнего дня, если не получиться в этом календаре, тогда можно попробовать заменить его.
- Есть файл с названием "delivery-test/HTML/layout-example.html" его можно взять за основу лаяута, только настроить импорт повторяющихся блоков
(сейчас, в вёрстке, импорт работает с помощью jQuery, html разметка всех импортируемых блоков в папке "delivery-test/HTML/template", стили от этих блоков в файле common.css 
- Файл "delivery-test/HTML/in-dev.html" это страница 404, но по сути меняется только лаяут, код перенесен со старой страницы.
- В вёрстке Пагинация не реализована, думал использовать текущую которая уже есть, перенесешь её, пожалуйста, а я её стилизую под новый дизайн.
- На странице Тарміни доставки есть таблица "Каталог напрямків", можешь плз перенести её старую, какая есть сейчас, в новый лаяут, и я её стилизую ("delivery-test/HTML/cl-delivery-time.html" )
- Все изображения уже сжаты в размере
- В формах, в инпутах и т.д я старался переносить html атрибуты(id,name и т.д, кроме старых классов) из текущих, но всё равно при переносе логики нужно их перепроверить, скорей всего пропустил что-то.
- Наверно было бы здорово отказаться от бутстрапа, если есть возможность конечно, его стили могут перебивать common.css,
я встречал его только в мега меню,  Если что-то нужно будет переверстать без бутстрапа - пиши.
- В сложных формах таких как "Забирання і доставка вантажу" к примеру, есть появляющиеся блоки при клике, в старой версии они были всплывающими окнами, поп-апами.
Кнопки "Додати посилку", "Додати склад отримання та отримувача" сейчас при клике открывается и закрывается эта форма,
тут нужно будет сделать возможность добавить несколько "Посылок или складов" так же как и в старой версии этих страницы.








Если будут вопросы - пиши)